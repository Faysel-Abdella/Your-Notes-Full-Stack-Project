import { styled } from "styled-components";

const Wrapper = styled.section`
  .input-container {
    position: relative;
  }
  .input {
  }

  .eye-icon {
    cursor: pointer;
    position: absolute;
    margin-left: -28px;
    margin-top: 12px;
    font-size: 20px;
    color: #aa3780;
  }
`;

export default Wrapper;
